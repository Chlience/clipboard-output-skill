# Demo

This directory contains a generated GIF for the GitHub README.

Render it from the repository root:

```bash
python3 demo/render_demo.py
```

The output is:

```text
demo/clipboard-output-demo.gif
```

The renderer uses only Python standard library code plus `ffmpeg`.
